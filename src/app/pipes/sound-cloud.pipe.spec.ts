import { SoundCloudPipe } from './sound-cloud.pipe';

describe('SoundCloudPipe', () => {
  it('create an instance', () => {
    const pipe = new SoundCloudPipe();
    expect(pipe).toBeTruthy();
  });
});
